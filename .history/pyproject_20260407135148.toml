[project]
name = "logicfp-citation-gateway"
version = "0.1.0"
description = "Citation gateway for logicfp knowledge base integration"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
